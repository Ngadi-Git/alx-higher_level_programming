#!/usr/bin/python3

def magic_calculation(a, b):
    from magic_calculatiion_102 import add, sub

    if a < b:
        sum = add(a, b)
        for index in range(4, 6):
            sum = ad    d(sum, index)
        return (sum)

    else:
        return(sub(a, b))
